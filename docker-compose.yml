version: '3.9'

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    environment:
      - DB_HOST=db
      - DB_NAME=devdb
      - DB_USER=devuser
      - DB_PASS=changeme
      - SECRET_KEY=1_dc7z50pjc^@3bngh2v%2^c=e=+o7!b!=f@wau%d+kr$rm)&texi
      - SHOPIFY_BASE_URL=https://antique-tableware.myshopify.com/admin/api/2023-01/
      - ACCESS_TOKEN=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

    depends_on:
      - db

  db:
    image: postgres:14-alpine
    volumes:
      - dev-db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=devdb
      - POSTGRES_USER=devuser
      - POSTGRES_PASSWORD=changeme
      - "TZ=Asia/Tokyo"

volumes:
  dev-db-data: